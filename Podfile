# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'nonhchoung_iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

    pod 'Alamofire'
    pod 'SwiftyJSON'
    pod 'ObjectMapper'

  # Pods for nonhchoung_iOS

  target 'nonhchoung_iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'nonhchoung_iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
